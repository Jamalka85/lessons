
from pytils.translit import slugify

def get_name(file):
    names = []
    for line in file:



def get_login(file):
    users = open(file)
    logins = []
    f = open(file)
    return f.readlines()
    # logins = []
    # for line in f:
    #     names = line.split('')
    #     login = names [0][0][ + names [1]

print get_login('users')